namespace LeetCode
{
    internal class _72EditDistance
    {
        public void Run()
        {
            string word1 = "horse";
            string word2 = "ros";

            Console.WriteLine(MinDistance(word1, word2));


        }

        public int MinDistance(string word1, string word2)
        {
            var lengthWord2 = word2.Length;

            for (int i = word1.Length - 1; i >= 0; i--)
            {
                //if (word1[])
            }

            return 0;
        }
    }
}
